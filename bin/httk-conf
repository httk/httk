#!/bin/bash

DIRNAME="$(dirname "$0")"
HTTK_BIN_PATH="$(cd "$DIRNAME"; pwd -P)"

if [ -z "$1" ]; then

    echo "binpath:$HTTK_BIN_PATH"

else

    for keys in "$@"; do
        if [ "$key" == "$binpath" ]; then
            echo "$HTTK_BIN_PATH"
        fi
    done

fi
